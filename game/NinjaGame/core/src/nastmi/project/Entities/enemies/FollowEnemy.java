package nastmi.project.Entities.enemies;

import com.badlogic.gdx.graphics.g2d.Sprite;
import nastmi.project.Entities.Enemy;
import nastmi.project.Entities.Player;
/*
public class FollowEnemy extends Enemy {

    private float speed;

    public FollowEnemy(int x, int y, float width, float height, float speed, int health, Sprite startSprite) {
        super(x, y, width, height, speed, health, startSprite);
        super.setCurrentSpeedX(speed);
        super.setCurrentSpeedY(0);
        this.speed = speed;
    }

   /* public void calculateNextSpeed(Player player){
        if(super.getRect().getX() < player.getRect().getX())
            super.setCurrentSpeedX(-speed);
        else
            super.setCurrentSpeedX(speed);
        super.setCurrentSpeedY();
    }*/


//}
